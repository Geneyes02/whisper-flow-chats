name: apply native history integration

on:
  pull_request:
    paths:
      - "scripts/apply_native_history_patch.py"
      - ".github/workflows/apply-native-history-pr.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - name: Apply asserted history patch
        run: python scripts/apply_native_history_patch.py
      - name: Format
        run: bun x prettier --write src/lib/native-mls-client.ts src/routes/_authenticated/app.tsx src/lib/native-local-history.ts
      - name: Typecheck
        run: bun x tsc --noEmit
      - name: Commit integration if changed
        shell: bash
        run: |
          if git diff --quiet; then
            echo "native history integration already applied"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/native-mls-client.ts src/routes/_authenticated/app.tsx src/lib/native-local-history.ts
          git commit -m "feat(e2ee): persist native chat history encrypted at rest"
          git push origin HEAD:${{ github.head_ref }}
