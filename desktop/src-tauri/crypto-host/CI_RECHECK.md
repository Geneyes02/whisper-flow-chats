# Phase B CI recheck

This marker intentionally triggers the crypto-host workflow after the OpenMLS 0.8.1 API, formatting, harness-import, strict-clippy, hostile-ciphertext containment, revocation-status, payload, attachment, and local-history fixes landed. It contains no runtime code.

Final direct-message promotion gate recheck: 2026-07-24.
