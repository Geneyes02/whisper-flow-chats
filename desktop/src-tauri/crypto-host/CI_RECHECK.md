# Phase B CI recheck

This marker intentionally triggers the crypto-host workflow after the OpenMLS 0.8.1 API, formatting, harness-import, and strict-clippy fixes landed. It contains no runtime code.

Final direct-message gate recheck: 2026-07-23.
